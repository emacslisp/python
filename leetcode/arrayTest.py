# Create a sample 2D list
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]

# Calculate the number of rows in the 2D list
num_rows = len(matrix)
num_length = len(matrix[0])

print("Number of rows:", num_rows)
print("Number of rows:", num_length)
